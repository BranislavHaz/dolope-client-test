import * as $ from "@/styles/layouts/configurator/Footer.styled";

const Footer = () => {
  return (
    <$.Footer>
      <$.BackButton>zpět</$.BackButton>
      <$.NextButton>pokračovat</$.NextButton>
    </$.Footer>
  );
};

export default Footer;
